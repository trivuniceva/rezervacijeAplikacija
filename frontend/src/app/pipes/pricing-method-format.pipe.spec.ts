import { PricingMethodFormatPipe } from './pricing-method-format.pipe';

describe('PricingMethodFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new PricingMethodFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
